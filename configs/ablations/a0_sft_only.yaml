# A0: SFT-only baseline. No RL - evaluate the SFT checkpoint directly.
# `skip_grpo` makes grpo_train.py exit with an explanation rather than train.
extends: ../grpo_base.yaml
use_spm: false
use_icr: false
use_dsac: false
skip_grpo: true
