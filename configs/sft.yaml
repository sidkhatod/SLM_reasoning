base_model: Qwen/Qwen2.5-7B
quantization: 4bit
lora_r: 64
lora_alpha: 128
lora_dropout: 0.05
lora_target_modules: [q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj]
learning_rate: 2e-4
lr_schedule: cosine
max_seq_length: 512
gradient_checkpointing: true
# batch_size and epochs left as null/placeholder — to be tuned empirically on real hardware
batch_size: null
epochs: null
